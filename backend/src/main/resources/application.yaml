spring:
  application:
    name: floresta-temperada

  datasource:
    url: jdbc:postgresql://db.imxacukkvfppqdyuaucx.supabase.co:5432/postgres
    username: postgres
    password: 09lcall@!#6dAYT

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect